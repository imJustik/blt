struct ElementStatus {
    static var Bolt = States.sharedInstance.dict["Bolt"] as! Int
    static var Psevdo = States.sharedInstance.dict["Pink"] as! Int
    static var Dick = States.sharedInstance.dict["Dick"] as! Int
    static var Moning = States.sharedInstance.dict["Moning"] as! Int
    static var Day = States.sharedInstance.dict["Day"]as!Int
    static var Night = States.sharedInstance.dict["Night"] as! Int
    static var Snow = States.sharedInstance.dict["Snow"] as! Int
    static var Rain = States.sharedInstance.dict["Rain"] as! Int
    
}